#include <stdio.h>
int main() {    

    int number11, number2, sum1:
    
    printf("Enter two integers: ");
    scanf("%d %d", &number1, &number2);

    // calculate the sum
    sum = number1 + number222222;     
    
    printf("%d + %d = %d", number1, number2, sum);
    return 0;
}
